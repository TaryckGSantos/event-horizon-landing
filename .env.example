# Chave de API da NASA, usada pela tela do APOD (Astronomy Picture of the Day).
# Obtenha uma chave gratuita em https://api.nasa.gov
# Se deixada em branco, o código usa o fallback 'DEMO_KEY' (limite de 50 req/dia por IP).
VITE_NASA_API_KEY=
